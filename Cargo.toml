[workspace]
members = [
    "orchestrator",
    "common",
]
exclude = [
    "ebpf",
]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
opt-level = 2
debug = false
strip = "debuginfo"
panic = "abort"
